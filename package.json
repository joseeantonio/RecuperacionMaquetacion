{
  "name": "my-project",
  "source": "src/index.html",
  "browserslist": "> 0.3%, last 3 versions, not dead",
  "scripts": {
    "start": "parcel serve src/index.html",
    "deploy": "parcel build --public-url /RecuperacionMaquetacion/ src/index.html && gh-pages build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.1",
    "gh-pages": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "parcel": "latest",
    "rimraf": "^3.0.2"
  }
}
